apiVersion: v1
kind: Service
metadata:
  name: claude-proxy
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: claude-proxy
  ports:
    - name: http
      port: 8090
      targetPort: 8090
