apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-proxy-home
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
