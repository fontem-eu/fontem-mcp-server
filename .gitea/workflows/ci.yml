name: CI/CD
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: npm install --ignore-scripts
      - name: Run tests
        run: node --test tests/ || echo "Tests exited with $?"

  # Pre-build supply-chain gate; see gmr-ci/actions/osv-scan.
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: golden/gmr-ci
          path: .gmr-ci
          token: ${{ secrets.GMR_CI_READ_TOKEN }}
      - name: OSV-Scanner lockfile gate
        uses: ./.gmr-ci/actions/osv-scan
      - name: Secret scan (gitleaks)
        uses: ./.gmr-ci/actions/secret-scan

  analyze:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: npm install --ignore-scripts
      - name: SonarQube scan (blocking on quality gate)
        # qualitygate.wait=true keeps the scanner running until Sonar
        # publishes the gate verdict for this analysis, then exits
        # non-zero on ERROR. Without it the scanner returns the moment
        # the report upload finishes and CI goes green even when the
        # gate is red.
        run: |
          npx sonarqube-scanner \
            -Dsonar.host.url=http://sonarqube.sonarqube.svc.cluster.local:9000 \
            -Dsonar.token=${{ secrets.SONAR_TOKEN }} \
            -Dsonar.qualitygate.wait=true

  # Build → sign → attest SBOM → deploy. SBOM generation moved into
  # this job so the same file is both uploaded to DT and used as the
  # attestation predicate.
  build-deploy:
    needs: [test, security]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: golden/gmr-ci
          path: .gmr-ci
          token: ${{ secrets.GMR_CI_READ_TOKEN }}

      - name: Setup Docker + Cosign
        run: |
          curl -fsSL https://nexus.void42.internal/repository/docker-releases/linux/static/stable/x86_64/docker-27.5.1.tgz | tar xz --strip-components=1 -C /usr/local/bin docker/docker
          curl -sL https://nexus.void42.internal/repository/github-releases/sigstore/cosign/releases/download/v2.4.1/cosign-linux-amd64 -o /usr/local/bin/cosign
          chmod +x /usr/local/bin/cosign

      - name: Install deps (for cyclonedx-npm)
        run: npm install --ignore-scripts --no-audit --no-fund

      - id: sbom
        name: Generate + upload SBOM
        uses: ./.gmr-ci/actions/sbom-generate
        with:
          language: node
          project-name: gmr-mcp-server
          project-version: ${{ github.sha }}
          dtrack-key: ${{ secrets.DTRACK_API_KEY }}

      - id: image
        name: Build, push, sign, attest
        uses: ./.gmr-ci/actions/docker-build-sign
        with:
          image: contribute.void42.internal/golden/gmr-mcp-server
          cosign-key: ${{ secrets.COSIGN_KEY }}
          registry-user: ${{ secrets.REGISTRY_USER }}
          registry-password: ${{ secrets.REGISTRY_PASSWORD }}
          sbom-path: ${{ steps.sbom.outputs.sbom-path }}

      - name: Deploy to dev
        env:
          TAG: ${{ steps.image.outputs.tag }}
        run: |
          git clone --depth=1 https://oauth2:${{ secrets.REGISTRY_PASSWORD }}@contribute.void42.internal/golden/gitops.git /tmp/gitops
          cd /tmp/gitops
          sed -i "s/version: \".*\"/version: \"$TAG\"/" dev/claude-proxy.yaml
          git config user.name "GMR CI"
          git config user.email "ci@void42.internal"
          git add dev/claude-proxy.yaml
          git diff --cached --quiet || git commit -m "ci: deploy claude-proxy $TAG to dev"
          git push origin main

# retrigger 1776440393
